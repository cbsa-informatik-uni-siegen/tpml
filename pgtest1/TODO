TODO: 

22.12.2005


 - Modus hinzufügen, wo nur noch die Axiome angegeben werden für Smallstep
   Interpreter

 - Button für den Syntaktischen Zucker hinzufügen, der den SZ auflöst.

 - && und || korrigieren.


03.17.2006

 GUI:
 - Bei TypeChecker einklappen einbauen (anhand der Expression zuordnen)

 GUI/Backend:
 - GUESS Regel einbauen in TypeChecker
 - ABSTR' Regel, wo Typ angegeben werden kann
 - Bei P-ID den Benutzer Instanzen für die Typvariablen wählen lassen.
 - Mehrstelliges let und lambda als synt. Zucker einbauen
   und curryfiziertes let (let rec id = id1 id2 id3...idn.e1 in e2)
 - Paare und Listen, dafür keine n-Tupel
 - Auflösen von synt. Zucker jeweils pro Knoten (nie rekursiv, sondern
   immer nur auf den äußersten Ausdruck), auch im Smallstep

 Backend:
 - Gebundene Umbenennung bei Substitution PIs


06.04.06

 typing.ProofTree:
 - guess() unifiziert nicht korrekt, z.B. bei [f:'a1] > f :: ('a0 -> 'a2)
   wenn (GUESS) angewendet wird, z.B. mit (int -> int), bleibt f:'a1 erhalten.
   Hierfür müsste man schon teilweise die Logik von (ID) einbauen, um die
   Verbindung zwischen 'a1 und ('a0 -> 'a2) herzustellen.
   
   
028.05.2006

 - Hilfsklasse für "gebundene Umbennenung"
 - SyntacticSugar als Annotation
 - Listen: cons wie gehabt, :: + [x1,..,xn] als syntaktischer Zucker